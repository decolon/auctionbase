--Drops the trigger that does not let up update the Auction start
--This trigger simple does not allow updating Auction start, so there is nothing it will let through
--will simply delete the trigger
drop trigger updateAuctionStart;
